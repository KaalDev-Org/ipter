# IPTER Application Environment Variables
# Copy this file to .env and update the values as needed

# Google Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
# This is REQUIRED for the application to work properly
GEMINI_API_KEY=your-gemini-api-key-here

# JWT Secret for authentication
# Generate a secure random string for production (at least 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-secure-jwt-secret-key-here

# PostgreSQL Database Configuration (optional)
# Only needed if you uncomment the PostgreSQL service in docker-compose.yml
POSTGRES_PASSWORD=your-secure-postgres-password

# Optional: Override default database settings for PostgreSQL
# Uncomment these if using PostgreSQL instead of H2
# SPRING_DATASOURCE_URL=jdbc:postgresql://ipter-db:5432/ipter
# SPRING_DATASOURCE_USERNAME=ipter
# SPRING_DATASOURCE_PASSWORD=your-secure-postgres-password

# Optional: Override file storage directories
# FILE_UPLOAD_DIR=/app/uploads
# FILE_PROCESSED_DIR=/app/processed
# FILE_REPORTS_DIR=/app/reports
# FILE_THUMBNAILS_DIR=/app/thumbnails

# Optional: Override user management settings
# USER_MAX_CONCURRENT_USERS=10
# USER_SESSION_TIMEOUT=7200000

# Optional: Override logging levels
# LOGGING_LEVEL_COM_IPTER=DEBUG
# LOGGING_LEVEL_ROOT=INFO

# Optional: Override CORS settings (add your domain)
# CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:80,https://yourdomain.com
