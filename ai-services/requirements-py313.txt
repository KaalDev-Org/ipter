# Python 3.13 compatible requirements
# Use pre-compiled wheels where possible

# Core web framework
fastapi
uvicorn[standard]
python-multipart

# Image processing (use latest versions for Python 3.13 compatibility)
numpy>=1.26.0
pillow>=10.0.0

# OCR
pytesseract

# OpenCV (headless version is more reliable)
opencv-python-headless

# Data handling
pydantic>=2.0.0
requests
python-dotenv
