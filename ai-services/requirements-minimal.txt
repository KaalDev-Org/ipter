# Minimal requirements for Windows installation
# Install these one by one if requirements.txt fails

# Core web framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Essential for image processing
numpy==1.24.3
pillow==9.5.0

# OCR
pytesseract==0.3.10

# OpenCV (try headless version if regular fails)
opencv-python==4.8.1.78
# opencv-python-headless==4.8.1.78

# Data handling
pydantic==2.5.0
requests==2.31.0
python-dotenv==1.0.0
